name: searchbar_animation
description: Searchbar Animation provides the horizontal slide animation to a searchbar and it is fully customizable.
version: 0.0.4
homepage: https://github.com/Anuj2799/searchbar_animation

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
